[project]
name = "crewai-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crewai[tools]>=1.15.2",
]
[tool.uv]
environments = ["sys_platform == 'win32'"]
